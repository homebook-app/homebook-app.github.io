# Homebook Developer Setup

```bash
# 1) Install dependencies
npm install

# 2) Start the development server
npm run dev

# 3) Run code quality checks
npm run typecheck
npm run lint

# 4) Generate static output for GitHub Pages
npm run generate
```

The development server runs on `http://localhost:3000`.
